# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'OrderTracker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OrderTracker
  pod 'EFColorPicker', :path => "frameworks/EFColorPicker-master"

  target 'OrderTrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OrderTrackerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
